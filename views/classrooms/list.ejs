<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Classrooms - benchWalaAi</title>
    <link rel="stylesheet" href="/css/list.css" />
  </head>
  <body>
    <%- include('../partials/header') %>
    <div class="page-container">
      <div class="page-header">
        <h2>All Classrooms</h2>
        <a href="/classrooms/create" class="btn-primary">
          ➕ Add New Classroom
        </a>
      </div>

      <div class="table-card">
        <table class="classrooms-table">
          <thead>
            <tr>
              <th>Room</th>
              <th>Floor</th>
              <th>Department</th>
              <th>Total Benches</th>
            </tr>
          </thead>
          <tbody>
            <% if (classrooms.length === 0) { %>
            <tr>
              <td colspan="4" class="empty-state">
                No classrooms added yet. Create your first one!
              </td>
            </tr>
            <% } else { %> <% classrooms.forEach(c => { %>
            <tr>
              <td><strong><%= c.name %></strong></td>
              <td><%= c.floor !== null ? c.floor : '-' %></td>
              <td><%= c.department || '-' %></td>
              <td class="bench-count"><%= c.total_benches %></td>
              <td>
                <a href="/classrooms/:<%= c.name %>/delete">DELETE</a>
              </td>
            </tr>
            <% }) %> <% } %>  
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
